<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.65*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="0.45*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#2e2f37"              >
            <Grid.RowDefinitions>
                <RowDefinition Height="0.05*" />
                <RowDefinition Height="0.90*" />
                <RowDefinition Height="0.05*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Name="SearchTextBox"/>
                <Button Grid.Column="1" 
                        Name="SearchButton" 
                        Content="Search"
                        Click="Search"/>
            </Grid>
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Visible"
                          HorizontalScrollBarVisibility="Disabled"                       >
                <StackPanel Name="TaskList"
                            Margin="10,5,10,5"
                            CanVerticallyScroll="True"/>
            </ScrollViewer>

            <Button Grid.Row="2" Name="AddButton" Click="AddNewTask" Content="Add"/>
            
        </Grid>

        <GridSplitter Grid.Column="1" 
               ShowsPreview="False" Width="3"
               HorizontalAlignment="Center" 
               VerticalAlignment="Stretch" />

        <Grid Grid.Column="2" Background="#2e2f37">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.05*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="0.05*" />                
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0" Name = "TaskTitlen" FontSize="15"/>
            <TextBox Grid.Row="1" Name = "TaskDescription" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap"
                     Height="Auto"
                     VerticalScrollBarVisibility="Auto"/>
            <Button Grid.Row="2" Name="Save" Click="OnSaveTask" Content="Save"/>
        </Grid>
        
       
        
        
    </Grid>
</Window>
